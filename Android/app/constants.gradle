ext {
    // data types
    INT = 'int'
    BOOL = 'boolean'
    STRING = 'String'

    // values (general)
    TRUE = 'true'
    FALSE = 'false'
    NULL = 'null'

    // Publishable API Key for the Android Client
    STRIPE_API_CLIENT_KEY_FIELD = 'STRIPE_API_CLIENT_KEY'

    // Secret API Key for the Java Server
    STRIPE_API_SERVER_KEY_FIELD = 'STRIPE_API_SERVER_KEY'
}
